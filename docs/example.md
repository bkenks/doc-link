# Example doc

Used to manually verify `@doc:` resolution during development. Open a saved file, add `// @doc: example.md#example-section`, and click the CodeLens.

## Example section

If the anchor jump works, your cursor lands here.
